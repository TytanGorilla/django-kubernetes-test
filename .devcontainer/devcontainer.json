{
  "name": "My Codespace App",
  "build": {
    "dockerfile": "../Dockerfile"
  },
  "features": {
    "node": "20",
    "ghcr.io/devcontainers/features/kubectl-helm-minikube:1": {},
    "ghcr.io/devcontainers/features/docker-in-docker:2.12.0": {}  // ✅ Ensures Docker is inside the container
  },
  "mounts": [
    "source=/var/run/docker.sock,target=/var/run/docker.sock,type=bind"
  ],
  "postCreateCommand": "
    echo '📌 Checking Docker availability...';
    if ! command -v docker &> /dev/null; then
      echo '❌ Docker is not installed inside this container!';
      exit 1;
    fi;
    docker info;
    echo '✅ Docker is running inside the dev container!';
  "
}